# Server Port
server.port=9791

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/bookstore?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=15901590

# JPA & Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Optional: Logging SQL parameters (good for debugging)
spring.jpa.properties.hibernate.format_sql=true

# Optional: Enable SQL logging with parameter values
logging.level.org.hibernate.type.descriptor.sql=TRACE
debug=true

